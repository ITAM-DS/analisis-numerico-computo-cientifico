## Instructivo:

Se cuenta con un algoritmo que producirá matrices aleatorias simétricas positivas definidas de la dimensión deseada. esta dimensión está dada en el script por la variable n.

Para cambiar la dimensión deseada del cómputo, basta con cambiar la definición del valor n y jecutar en terminañ:

```
Rscript matriz.r

```

Para ejecutar el algoritmo se tendrá que compilar:

```
gcc -Wall -fopenmp source/chlesky_final.c source/funciones.c -o cholesky_final.out -lm
```

Para correr:
```
./cholesky_final.out
```

Este algoritmo hará el cómputo de la factorización pero no dará el tiempo.

Para poder obtener el tiempo de cómputo se deberá de ejecutar:

```
gcc -Wall -fopenmp source/chlesky.c source/funciones.c -o cholesky_final.out -lm
```

Para correr:
```
./cholesky_final.out
```