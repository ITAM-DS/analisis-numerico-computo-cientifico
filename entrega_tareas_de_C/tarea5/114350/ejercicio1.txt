La diferencia:

include<file> 

	Esta variante es usada para encabezados de sistema. Busca un archivo estandar en la lista de de los directoriosd de sistema.

inclued "file"

	Usado para encabezados de archivo de programación propia. Primero busca el archivo (file) en el directorio que contiene el archivo en cuestión o ejecutado.


Static storage class:

Esta declaración le dice al compilador que mantenga la existencia local de una variable mientras el programa en cuestión se ejecuta. Declarar de manera local las variables del estilo static permite mantener sus valores entre funciones. 
